{
    "version": 2,
    "installCommand": "git clone https://github.com/flutter/flutter.git -b stable && export PATH=$PWD/flutter/bin:$PATH && flutter --version && flutter config --enable-web && flutter pub get",
    "buildCommand": "export PATH=$PWD/flutter/bin:$PATH && flutter build web --release",
    "outputDirectory": "build/web",
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/index.html"
        }
    ]
}